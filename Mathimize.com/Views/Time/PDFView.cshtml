@model Mathimize.com.Models.TimeResultViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <h3>
        @Model.Op</h3>
    
    @{
        if (Model != null && Model.HourMinutes != null)
        {
        
            int i = 0;

            for (int r = 0; r < Model.Rows; r++)
            {
        <div class="row">
            @for (int c = 0; c < Model.Cols; c++)
            {
                <div class="span2 border time_box">
                    @Html.Partial("_" + Model.Op + "Result", Model.HourMinutes[i++])
                </div>
                    
            }
        </div>
            }

        }


    }
</div>
